language: node_js
sudo: false
node_js:
  - "8"

install:
  - npm install
  - bundle install

before_deploy:
  - touch mkdir -p build-prod && build-prod/fake.file

deploy:
  provider: script
  script: bash deploy.sh
  on:
    branch: fix-deploy
